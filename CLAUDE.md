# 마이빌더 ERP 웹 전환 프로젝트

## 프로젝트 개요
- **목적**: 기존 마이빌더(C/S) ERP를 웹 기반으로 전환
- **방식**: 기초 MASTER 화면은 공통 템플릿으로 표준화, 회사별 업무 화면은 표준 화면 수정 또는 신규 개발

## 기술 스택
- **Frontend**: React 19, TypeScript, Vite
- **UI 라이브러리**: Ant Design (antd) v6
- **그리드**: AG Grid v35 (ag-grid-react)
- **라우팅**: React Router v7
- **상태관리**: Zustand v5
- **차트**: Recharts v3
- **날짜**: dayjs
- **Backend**: Express v5 + MSSQL (Node.js, server/ 디렉터리)
- **개발 서버 실행**: `npm run dev` (프론트), `npm run server` (백엔드)

## 화면 정의서 명명 규칙

| 접두사 | 대상 화면 | 정의서 위치 | 예시 |
|--------|-----------|------------|------|
| `MDI_` | 로그인, 메인 화면, 메인 메뉴 트리 등 시스템 프레임 화면 | `docs/LOGIN/` 또는 `docs/MDI/` | `MDI_LOGIN.md`, `MDI_MAIN.md`, `MDI_MAIN_TREE.md` |
| `SCR-도메인-번호` | 일반 업무 화면 (기준정보, 영업, 구매 등) | `docs/screens/` | `SCR-MST-001.md`, `SCR-SAL-001.md` |

- `MDI_` 화면은 로그인·메인 프레임 관련 화면에만 사용합니다.
- 일반 업무 화면은 기존 `SCR-도메인-번호` 규칙을 유지합니다.
- 구현 파일명도 정의서 ID와 일치시킵니다 (예: `MDI_LOGIN.tsx`, `SCR-MST-001.tsx`).

## 개발 워크플로우 (필수 준수)
1. 화면 구현 전 반드시 정의서를 먼저 읽을 것
   - `MDI_` 화면: `docs/LOGIN/` 또는 `docs/MDI/`
   - `SCR-` 화면: `docs/screens/`
2. 화면 정의서가 없는 화면은 구현하지 말고, `docs/templates/`의 템플릿으로 정의서 초안 작성 후 사용자 확인을 받을 것
3. 정의서에 없는 기능은 임의로 추가하지 말고 사용자에게 확인할 것
4. 구현 중 정의서와 달라진 내용이 생기면 정의서도 함께 업데이트할 것
5. 모든 화면은 아래 공통 화면 패턴을 기반으로 구현할 것

## 화면 패턴 분류
| 유형 | 설명 | 예시 |
|------|------|------|
| `MASTER-CRUD` | 기준정보 단일 그리드 등록/조회 화면 | 거래처, 품목, 사원 |
| `HEADER-DETAIL` | 전표형 헤더+디테일 입력 화면 | 수주, 발주, 출하 |
| `INQUIRY` | 조회/리포트 화면 (검색조건 + 결과 그리드/차트) | 매출현황, 재고현황 |
| `POPUP` | 코드도움/검색 팝업 | 거래처검색, 품목검색 |
| `DASHBOARD` | 현황판 | 영업현황, 생산현황 |

## 공통 참조 문서
- 프로젝트 개요·기술 스택: `docs/00-overview.md`
- 데이터베이스 규칙: `docs/01-database.md`
- 공통 UI/UX 규칙: `docs/02-ui-standards.md`

## 금지 사항
- 정의서 없는 화면 구현 금지
- DB 스키마 임의 변경 금지 (변경 필요 시 사용자 확인)
- 공통 컴포넌트를 우회한 화면별 일회성 구현 금지
